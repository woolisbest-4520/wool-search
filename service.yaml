apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: my-search-engine
spec:
  template:
    spec:
      containers:
        - image: gcr.io/PROJECT_ID/my-search-engine
          ports:
            - containerPort: 8000
          resources:
            limits:
              memory: 256Mi
              cpu: "1"

name: wool-search
services:
  - name: web
    type: worker
    image: python:3.11-slim
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - port: 8000
        protocol: http
    regions:
      - fra 
    env:
      - key: PORT
        value: "8000"
